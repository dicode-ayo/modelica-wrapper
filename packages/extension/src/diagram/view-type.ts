/**
 * The `modelica.diagram` custom-editor viewType. Kept in its own module so both
 * the provider and `openDiagram` can reference it without an import cycle.
 */
export const DIAGRAM_VIEW_TYPE = "modelica.diagram";
