{
  "name": "@modelica-wrapper/result-ui",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone Lit + ECharts custom elements (<om-*>) for the postprocessing / results view. Independent of the diagram editor (no Babylon).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "happy-dom": "^15.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
