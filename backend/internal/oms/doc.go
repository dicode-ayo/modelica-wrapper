// Package oms wraps OMSimulator's liboms via cgo.
//
// Used for FMU composition (SSP) and co-simulation. liboms is statically
// linked or loaded as a shared library; the C API is in oms.h.
package oms
